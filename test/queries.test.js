const queries = require('../db/queries');


describe('queries', () => {
  
 
    const managerSelected = 3
    
        // Arrange
        test('results fromd database', () => {
          expect(queries.findAllEmployeesByManager(managerSelected)).toBe(


          );
        });
    

  
      
    }
)